# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
backend/build/
.pytest_cache/
.ruff_cache/

# Data / runtime
data/
*.db
*.db-wal
*.db-shm
*.log
config.json
users.json
.env

# olcrtc binary (provided per-host, amd64) + its recorded version
olcrtc-linux-amd64
olcrtc-linux-amd64.version

# Node / frontend
node_modules/
*.tsbuildinfo
*.local
# NOTE: frontend/dist IS committed so the installer needs no Node on the server.
# CI rebuilds it on every push; maintainers can also run `npm --prefix frontend run build`.

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Temporary WDTT integration material (remove later)
tempwdtt/
